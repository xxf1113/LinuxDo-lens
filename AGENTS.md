# AGENTS.md

## Git 自动提交规则

- 每次修改文件后，必须自动执行 Git 提交并推送到 GitHub：
  1. `git add <修改的文件>`
  2. `git commit -m "<描述性提交信息>"`
  3. `git push`
- 提交信息使用简体中文，简要描述本次修改内容。
- 如果 `git push` 失败（网络问题、认证失败等），必须如实报告失败原因，不得假装已提交成功。

## 输出语言

- 所有回复使用简体中文。
